import 'package:flutter/material.dart';

class adminLogin extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Hello sa imong tanan'),
      ),
      body: Center(
        child: Text('hilu bai'),
      ),
    );
  }
}
